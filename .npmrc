# React Native / Metro (used by apps/mobile) assumes packages like
# @babel/runtime, whatwg-fetch, etc. are hoisted to the root node_modules,
# which pnpm's default strict isolation does not do. Hoisting avoids
# whack-a-mole "module not found" errors when bundling with Metro.
shamefully-hoist=true
